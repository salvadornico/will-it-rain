{
	"name": "will-it-rain",
	"version": "1.0.0",
	"main": "public/index.html",
	"license": "MIT",
	"scripts": {
		"build": "yarn run tsc && yarn run insert-key",
		"tsc": "tsc src/get-rain.ts --outDir scripts",
		"insert-key": "sed -i s/DARKSKY_TOKEN/$DARKSKY_TOKEN/g scripts/get-rain.js"
	},
	"devDependencies": {
		"@types/aws-lambda": "^8.10.10",
		"typescript": "^3.0.1"
	}
}